export type Catalog = {
    link: string,
    progressList: Array<number>,
    trophyProgress: number,
    starredTrophyProgress: number,
    title: string,
    titleTranslation?: string,
    note?: string,
}